import { IconStyleDirective } from './icon-style.directive';

describe('IconStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new IconStyleDirective();
    expect(directive).toBeTruthy();
  });
});
